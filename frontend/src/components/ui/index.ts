/**
 * UI Component Library Exports
 * 
 * This library provides 12+ reusable components:
 * - Card (with Header, Title, Description, Content, Footer)
 * - Button
 * - Modal (with Header, Title, Description, Body, Footer)
 * - Input (with Textarea)
 * - Select
 * - EmptyState
 * - Spinner (with LoadingOverlay, PageLoading)
 * - Badge (with StatusBadge)
 * - Skeleton (with SkeletonText, SkeletonCard, SkeletonAvatar, SkeletonTable, SkeletonStatCard)
 * - Avatar (with AvatarGroup)
 * - Pagination (with PaginationInfo)
 * - Tooltip
 * - Dropdown
 * - ConfirmDialog
 */

// Card
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from './Card'

// Button
export { Button } from './Button'

// Modal
export {
  Modal,
  ModalHeader,
  ModalTitle,
  ModalDescription,
  ModalBody,
  ModalFooter,
} from './Modal'

// Input
export { Input, Textarea } from './Input'

// Select
export { Select } from './Select'

// EmptyState
export { EmptyState } from './EmptyState'

// Spinner / Loading
export { Spinner, LoadingOverlay, PageLoading } from './Spinner'

// Badge
export { Badge, StatusBadge } from './Badge'

// Skeleton
export {
  Skeleton,
  SkeletonText,
  SkeletonCard,
  SkeletonAvatar,
  SkeletonTable,
  SkeletonStatCard,
} from './Skeleton'

// Avatar
export { Avatar, AvatarGroup } from './Avatar'

// Pagination
export { Pagination, PaginationInfo } from './Pagination'

// Tooltip
export { Tooltip } from './Tooltip'

// Dropdown
export { Dropdown } from './Dropdown'

// ConfirmDialog
export { ConfirmDialog } from './ConfirmDialog'
